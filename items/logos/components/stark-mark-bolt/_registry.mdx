---
name: stark-mark-bolt
type: registry:ui
title: Stark Bolt
description: An original Stark app mark drawn as a rounded-square icon in a clean, minimal, bold visual language.
meta:
  category: "DevOps"
---

A self-contained SVG mark. It sizes with `className`, so it drops into an app grid, a nav, or a
favicon-scale slot without extra wrapping.

```tsx
import { StarkMarkBolt } from "@/components/ui/stark-mark-bolt";

<StarkMarkBolt className="size-16" />
```
