---
name: guarahooks
type: registry:item
title: "guarahooks"
description: "100 React hooks installable through the shadcn CLI — state, DOM, network and more."
meta:
  sourceKind: "registry"
  project: "guarahooks"
  author: "h3rmel"
  license: "MIT"
  sourceUrl: "https://github.com/h3rmel/guarahooks"
  homepage: "https://guarahooks.com"
  tags: "hooks, utilities, react"
  command: "npx shadcn@latest add https://guarahooks.com/r/use-window-size.json"
  note: "Publishes its own shadcn registry (100 hooks), so installs come straight from them."
---

100 React hooks installable through the shadcn CLI — state, DOM, network and more.

Source: [guarahooks](https://github.com/h3rmel/guarahooks) by h3rmel — MIT.

Publishes its own shadcn registry (100 hooks), so installs come straight from them.

```bash
npx shadcn@latest add https://guarahooks.com/r/use-window-size.json
```
