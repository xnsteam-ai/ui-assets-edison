---
name: empty-state-page
type: registry:block
title: Empty State Page
description: A centered empty state with artwork slot, message, and recovery action.
files:
  - path: empty-state-page.tsx
    type: registry:block
---

**Placeholder layout.** This template ships a blocked-out structure rather than finished
content. Use it to reserve the layout, then replace each region with real components.

```tsx
import { EmptyStatePage } from "@/components/empty-state-page";

<EmptyStatePage />
```
