---
name: landing-hero
type: registry:block
title: Landing Hero
description: A centered hero with headline, supporting copy, and a primary/secondary action pair.
files:
  - path: landing-hero.tsx
    type: registry:block
---

**Placeholder layout.** This template ships a blocked-out structure rather than finished
content. Use it to reserve the layout, then replace each region with real components.

```tsx
import { LandingHero } from "@/components/landing-hero";

<LandingHero />
```
