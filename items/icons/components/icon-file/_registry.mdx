---
name: icon-file
type: registry:ui
title: File
description: The File icon from the Stark icon set, drawn on a 24px grid with a 1.75 stroke.
---

Inherits `currentColor` and sizes with `className`, so it matches the text it sits beside.

```tsx
import { IconFile } from "@/components/ui/icon-file";

<IconFile className="size-5" />
```
