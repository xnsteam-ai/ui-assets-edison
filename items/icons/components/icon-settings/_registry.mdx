---
name: icon-settings
type: registry:ui
title: Settings
description: The Settings icon from the Stark icon set, drawn on a 24px grid with a 1.75 stroke.
---

Inherits `currentColor` and sizes with `className`, so it matches the text it sits beside.

```tsx
import { IconSettings } from "@/components/ui/icon-settings";

<IconSettings className="size-5" />
```
