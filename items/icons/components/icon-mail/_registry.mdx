---
name: icon-mail
type: registry:ui
title: Mail
description: The Mail icon from the Stark icon set, drawn on a 24px grid with a 1.75 stroke.
---

Inherits `currentColor` and sizes with `className`, so it matches the text it sits beside.

```tsx
import { IconMail } from "@/components/ui/icon-mail";

<IconMail className="size-5" />
```
