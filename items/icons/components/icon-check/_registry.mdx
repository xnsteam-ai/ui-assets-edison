---
name: icon-check
type: registry:ui
title: Check
description: The Check icon from the Stark icon set, drawn on a 24px grid with a 1.75 stroke.
---

Inherits `currentColor` and sizes with `className`, so it matches the text it sits beside.

```tsx
import { IconCheck } from "@/components/ui/icon-check";

<IconCheck className="size-5" />
```
