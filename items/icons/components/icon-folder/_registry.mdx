---
name: icon-folder
type: registry:ui
title: Folder
description: The Folder icon from the Stark icon set, drawn on a 24px grid with a 1.75 stroke.
---

Inherits `currentColor` and sizes with `className`, so it matches the text it sits beside.

```tsx
import { IconFolder } from "@/components/ui/icon-folder";

<IconFolder className="size-5" />
```
