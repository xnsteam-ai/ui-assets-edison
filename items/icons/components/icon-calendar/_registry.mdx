---
name: icon-calendar
type: registry:ui
title: Calendar
description: The Calendar icon from the Stark icon set, drawn on a 24px grid with a 1.75 stroke.
---

Inherits `currentColor` and sizes with `className`, so it matches the text it sits beside.

```tsx
import { IconCalendar } from "@/components/ui/icon-calendar";

<IconCalendar className="size-5" />
```
