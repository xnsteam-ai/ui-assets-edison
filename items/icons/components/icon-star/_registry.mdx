---
name: icon-star
type: registry:ui
title: Star
description: The Star icon from the Stark icon set, drawn on a 24px grid with a 1.75 stroke.
---

Inherits `currentColor` and sizes with `className`, so it matches the text it sits beside.

```tsx
import { IconStar } from "@/components/ui/icon-star";

<IconStar className="size-5" />
```
