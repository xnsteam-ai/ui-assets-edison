---
name: icon-link
type: registry:ui
title: Link
description: The Link icon from the Stark icon set, drawn on a 24px grid with a 1.75 stroke.
---

Inherits `currentColor` and sizes with `className`, so it matches the text it sits beside.

```tsx
import { IconLink } from "@/components/ui/icon-link";

<IconLink className="size-5" />
```
