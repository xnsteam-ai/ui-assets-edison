---
name: icon-heart
type: registry:ui
title: Heart
description: The Heart icon from the Stark icon set, drawn on a 24px grid with a 1.75 stroke.
---

Inherits `currentColor` and sizes with `className`, so it matches the text it sits beside.

```tsx
import { IconHeart } from "@/components/ui/icon-heart";

<IconHeart className="size-5" />
```
