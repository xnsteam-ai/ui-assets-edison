---
name: icon-arrow-right
type: registry:ui
title: Arrow Right
description: The Arrow Right icon from the Stark icon set, drawn on a 24px grid with a 1.75 stroke.
---

Inherits `currentColor` and sizes with `className`, so it matches the text it sits beside.

```tsx
import { IconArrowRight } from "@/components/ui/icon-arrow-right";

<IconArrowRight className="size-5" />
```
