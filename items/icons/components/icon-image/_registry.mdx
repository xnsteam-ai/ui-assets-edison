---
name: icon-image
type: registry:ui
title: Image
description: The Image icon from the Stark icon set, drawn on a 24px grid with a 1.75 stroke.
---

Inherits `currentColor` and sizes with `className`, so it matches the text it sits beside.

```tsx
import { IconImage } from "@/components/ui/icon-image";

<IconImage className="size-5" />
```
