---
name: icon-eye
type: registry:ui
title: Eye
description: The Eye icon from the Stark icon set, drawn on a 24px grid with a 1.75 stroke.
---

Inherits `currentColor` and sizes with `className`, so it matches the text it sits beside.

```tsx
import { IconEye } from "@/components/ui/icon-eye";

<IconEye className="size-5" />
```
