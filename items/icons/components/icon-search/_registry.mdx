---
name: icon-search
type: registry:ui
title: Search
description: The Search icon from the Stark icon set, drawn on a 24px grid with a 1.75 stroke.
---

Inherits `currentColor` and sizes with `className`, so it matches the text it sits beside.

```tsx
import { IconSearch } from "@/components/ui/icon-search";

<IconSearch className="size-5" />
```
