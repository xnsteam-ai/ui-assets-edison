---
name: icon-plus
type: registry:ui
title: Plus
description: The Plus icon from the Stark icon set, drawn on a 24px grid with a 1.75 stroke.
---

Inherits `currentColor` and sizes with `className`, so it matches the text it sits beside.

```tsx
import { IconPlus } from "@/components/ui/icon-plus";

<IconPlus className="size-5" />
```
