---
name: icon-bell
type: registry:ui
title: Bell
description: The Bell icon from the Stark icon set, drawn on a 24px grid with a 1.75 stroke.
---

Inherits `currentColor` and sizes with `className`, so it matches the text it sits beside.

```tsx
import { IconBell } from "@/components/ui/icon-bell";

<IconBell className="size-5" />
```
