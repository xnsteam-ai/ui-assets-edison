---
name: icon-clock
type: registry:ui
title: Clock
description: The Clock icon from the Stark icon set, drawn on a 24px grid with a 1.75 stroke.
---

Inherits `currentColor` and sizes with `className`, so it matches the text it sits beside.

```tsx
import { IconClock } from "@/components/ui/icon-clock";

<IconClock className="size-5" />
```
