---
name: icon-upload
type: registry:ui
title: Upload
description: The Upload icon from the Stark icon set, drawn on a 24px grid with a 1.75 stroke.
---

Inherits `currentColor` and sizes with `className`, so it matches the text it sits beside.

```tsx
import { IconUpload } from "@/components/ui/icon-upload";

<IconUpload className="size-5" />
```
