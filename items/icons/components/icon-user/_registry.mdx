---
name: icon-user
type: registry:ui
title: User
description: The User icon from the Stark icon set, drawn on a 24px grid with a 1.75 stroke.
---

Inherits `currentColor` and sizes with `className`, so it matches the text it sits beside.

```tsx
import { IconUser } from "@/components/ui/icon-user";

<IconUser className="size-5" />
```
