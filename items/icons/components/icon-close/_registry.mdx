---
name: icon-close
type: registry:ui
title: Close
description: The Close icon from the Stark icon set, drawn on a 24px grid with a 1.75 stroke.
---

Inherits `currentColor` and sizes with `className`, so it matches the text it sits beside.

```tsx
import { IconClose } from "@/components/ui/icon-close";

<IconClose className="size-5" />
```
