---
name: icon-arrow-up-right
type: registry:ui
title: Arrow Up Right
description: The Arrow Up Right icon from the Stark icon set, drawn on a 24px grid with a 1.75 stroke.
---

Inherits `currentColor` and sizes with `className`, so it matches the text it sits beside.

```tsx
import { IconArrowUpRight } from "@/components/ui/icon-arrow-up-right";

<IconArrowUpRight className="size-5" />
```
