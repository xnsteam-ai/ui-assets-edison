---
name: icon-home
type: registry:ui
title: Home
description: The Home icon from the Stark icon set, drawn on a 24px grid with a 1.75 stroke.
---

Inherits `currentColor` and sizes with `className`, so it matches the text it sits beside.

```tsx
import { IconHome } from "@/components/ui/icon-home";

<IconHome className="size-5" />
```
