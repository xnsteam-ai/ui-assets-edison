---
name: icon-filter
type: registry:ui
title: Filter
description: The Filter icon from the Stark icon set, drawn on a 24px grid with a 1.75 stroke.
---

Inherits `currentColor` and sizes with `className`, so it matches the text it sits beside.

```tsx
import { IconFilter } from "@/components/ui/icon-filter";

<IconFilter className="size-5" />
```
