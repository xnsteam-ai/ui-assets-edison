---
name: illustration-all-done
type: registry:ui
title: All Done
description: A confirmation badge with a few sparks.
---

Line art that inherits `currentColor` for its ink, so it reads correctly in light and dark. Size it
with `className`.

```tsx
import { IllustrationAllDone } from "@/components/ui/illustration-all-done";

<IllustrationAllDone className="w-64 text-foreground" />
```
