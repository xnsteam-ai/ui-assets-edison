---
name: illustration-offline
type: registry:ui
title: Offline
description: No connection — signal arcs struck through.
---

Line art that inherits `currentColor` for its ink, so it reads correctly in light and dark. Size it
with `className`.

```tsx
import { IllustrationOffline } from "@/components/ui/illustration-offline";

<IllustrationOffline className="w-64 text-foreground" />
```
