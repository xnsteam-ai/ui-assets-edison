---
name: illustration-error-cloud
type: registry:ui
title: Error Cloud
description: Something broke upstream — a storm cloud with a bolt.
---

Line art that inherits `currentColor` for its ink, so it reads correctly in light and dark. Size it
with `className`.

```tsx
import { IllustrationErrorCloud } from "@/components/ui/illustration-error-cloud";

<IllustrationErrorCloud className="w-64 text-foreground" />
```
