---
name: illustration-no-results
type: registry:ui
title: No Results
description: A magnifier that came back empty.
---

Line art that inherits `currentColor` for its ink, so it reads correctly in light and dark. Size it
with `className`.

```tsx
import { IllustrationNoResults } from "@/components/ui/illustration-no-results";

<IllustrationNoResults className="w-64 text-foreground" />
```
