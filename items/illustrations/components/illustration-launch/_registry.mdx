---
name: illustration-launch
type: registry:ui
title: Launch
description: A small rocket on its way up.
---

Line art that inherits `currentColor` for its ink, so it reads correctly in light and dark. Size it
with `className`.

```tsx
import { IllustrationLaunch } from "@/components/ui/illustration-launch";

<IllustrationLaunch className="w-64 text-foreground" />
```
