---
name: illustration-empty-box
type: registry:ui
title: Empty Box
description: Nothing here yet — an open carton with a few pieces drifting out.
---

Line art that inherits `currentColor` for its ink, so it reads correctly in light and dark. Size it
with `className`.

```tsx
import { IllustrationEmptyBox } from "@/components/ui/illustration-empty-box";

<IllustrationEmptyBox className="w-64 text-foreground" />
```
